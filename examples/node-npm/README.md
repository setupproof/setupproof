# Node/npm Example

This example uses Node.js and npm only. It has no package dependencies, so the
quickstart does not run `npm install` or contact the package registry.

<!-- setupproof id=node-npm-test -->
```sh
npm test
```
